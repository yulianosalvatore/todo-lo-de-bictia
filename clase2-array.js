// arregis
var arrayNumber = [0, 1, 5, 6]
var arrayString = ["a", "c", "y"]
var arrayMixto = ["a", 9, {}, function () { }]

var arrayNumber = [0, 1, 5, 6]
//forma de saber el tamaño del array
arrayNumber.length //4

//Agrega un nuevo elemento 
arrayNumber.push(8)
arrayNumber.length
// ejercico

var entrada = ['las', 'frutas', 'son', 'muy', 'ricas']

for (var i = 0; i < entrada.length; i++) {
    if (i >= 0) {
        tu = entrada.charAt(0).toUpperCase()
        entrada.substr(0, length - 2).toLocaleLowerCase
    }
    console.log(` return ${entrada}`)
}
